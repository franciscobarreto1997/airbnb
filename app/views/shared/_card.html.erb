<% @flats.each do |flat| %>
    <div class="card">
      <%= link_to flat_path(flat) do %>
          <div class="pic" style="background-image: url('<%= cl_image_path flat.photo, crop: :fill %>')">
          </div>
          <h5>
            <%= flat.room_type.name.upcase %>
          </h5>
          <h6>
            <strong>
              <%= flat.title %>
            </strong>
          </h6>
          <p> €<%= flat.price %> per night</p>
    </div>
      <% end %>
<% end %>
